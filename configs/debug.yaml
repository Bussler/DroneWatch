defaults:
  - env: swarm_search_v1
  - model: ppo_feedforward
  - training: debug
  - logging: mlflow_local
  - rendering: default
  - tune: ray_tune

project:
  name: DebugDroneWatch
  seed: 133742
  output_dir: outputs
  artifact_dir: artifacts
  resolved_config_filename: resolved_config.yaml
