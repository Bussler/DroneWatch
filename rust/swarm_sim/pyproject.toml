[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "swarm-sim"
version = "0.1.0"
description = "Rust simulation extension for DroneWatch."
requires-python = ">=3.11"
license = "MIT"

[tool.maturin]
features = ["pyo3/extension-module"]
